export default class CartProduct {
    constructor(id, pName, price, quantity, mediaUrl) {
       this.id = id;
       this.pName = pName;
       this.price = price;
       this.quantity = quantity;
       this.mediaUrl = mediaUrl;
    }
 }